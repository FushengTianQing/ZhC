// debug_example.zhc
// 调试信息测试程序

结构体 点 {
    整数 x
    整数 y
}

函数 整数 计算距离(点 p1, 点 p2) {
    变量 dx = p2.x - p1.x
    变量 dy = p2.y - p1.y
    变量 距离 = dx * dx + dy * dy
    返回 距离
}

函数 主函数() {
    变量 p1 = 点{10, 20}
    变量 p2 = 点{30, 40}
    变量 结果 = 计算距离(p1, p2)
    打印("距离平方 = ", 结果)
    返回 0
}
