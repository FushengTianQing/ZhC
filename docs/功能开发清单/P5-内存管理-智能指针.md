# P5-内存管理-智能指针

> **优先级**: P5
> **功能模块**: 内存管理
> **功能名称**: 智能指针
> **创建日期**: 2026-04-10
> **状态**: 规划中

## 1. 功能概述

实现类似 C++ `unique_ptr`/`shared_ptr` 的智能指针功能，提供自动内存管理、所有权语义和循环引用检测。

### 1.1 目标

- 支持 `独享指针` (unique_ptr)
- 支持 `共享指针` (shared_ptr)
- 支持 `弱指针` (weak_ptr)
- 支持循环引用检测

### 1.2 语法设计

```zhc
// 独享指针 - 独占所有权
独享指针[整数型] ptr1 = 创建独享指针[整数型](42);
整数型 值 = ptr1.获取();           // 42
ptr1.重置(100);                   // 重置为新值
ptr1.释放();                      // 释放所有权

// 共享指针 - 共享所有权
共享指针[整数型] sp1 = 创建共享指针[整数型](42);
共享指针[整数型] sp2 = sp1;       // 共享所有权
整数型 值 = sp2.获取();           // 42
整数型 引用计数 = sp1.引用计数();  // 2
sp1.重置();                        // 引用计数变为 1

// 弱指针 - 不拥有所有权
弱指针[整数型] wp1 = sp1.转弱();
如果 (wp1.已过期()) {
    打印("对象已销毁");
} 否则 {
    共享指针[整数型] sp3 = wp1.锁定();  // 升级为共享指针
}

// 自定义删除器
函数型 关闭句柄(文件型*) = (文件型* f) -> {
    关闭文件(f);
};
共享指针[文件型] file_ptr = 创建共享指针[文件型](打开文件("test.txt"), 关闭句柄);

// 指针操作
整数型* 裸指针 = sp1.裸指针();
如果 (sp1) {
    打印("有效指针");
}
```

---

## 2. 现有项目分析

### 2.1 相关模块

| 模块 | 路径 | 现有功能 |
|------|------|----------|
| 类型系统 | `src/zhc/type_system/smart_ptr.py` | 智能指针类型检查 |
| 内存管理 | `src/zhc/memcheck/` | 内存追踪 |

### 2.2 现有智能指针检查

```python
# src/zhc/type_system/smart_ptr.py
class PointerType(Enum):
    UNIQUE = "独享指针"
    SHARED = "共享指针"
    WEAK = "弱指针"

@dataclass
class SmartPointerInfo:
    var_name: str
    type_name: str
    pointer_type: PointerType
    target_type: str
    ref_count: int = 1
```

### 2.3 缺失功能

- ❌ 智能指针运行时实现
- ❌ 引用计数管理
- ❌ 循环引用检测
- ❌ 自定义删除器

---

## 3. 技术实现方案

### 3.1 架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                    智能指针系统                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────┐      ┌─────────────────┐            │
│  │ ControlBlock    │      │ SmartPtrBase    │            │
│  │ - ref_count     │      │ - ptr           │            │
│  │ - weak_count    │      │ - control       │            │
│  │ - object        │      └─────────────────┘            │
│  │ - deleter       │               │                       │
│  └─────────────────┘               ▼                       │
│           │                ┌─────────────────┐            │
│           │                │ UniquePtr       │            │
│           │                │ SharedPtr       │            │
│           │                │ WeakPtr         │            │
│           │                └─────────────────┘            │
│           ▼                                                │
│  ┌─────────────────┐                                       │
│  │ CycleDetector   │                                       │
│  │ - detect()      │                                       │
│  └─────────────────┘                                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 核心数据结构

```python
# src/zhc/memory/smart_ptr.py

from dataclasses import dataclass, field
from typing import TypeVar, Generic, Optional, Callable, Any
from enum import Enum

T = TypeVar('T')

@dataclass
class ControlBlock:
    """控制块 - 管理引用计数"""
    object: Any                    # 管理的对象
    ref_count: int = 1            # 共享引用计数
    weak_count: int = 0           # 弱引用计数
    deleter: Optional[Callable] = None  # 自定义删除器
    type_info: Optional[str] = None  # 类型信息
    
    def add_ref(self) -> int:
        """增加引用计数"""
        self.ref_count += 1
        return self.ref_count
    
    def release(self) -> int:
        """减少引用计数，返回剩余计数"""
        self.ref_count -= 1
        if self.ref_count <= 0:
            self._destroy_object()
            self._release_weak()
        return self.ref_count
    
    def add_weak_ref(self) -> int:
        """增加弱引用计数"""
        self.weak_count += 1
        return self.weak_count
    
    def release_weak(self) -> int:
        """减少弱引用计数"""
        self.weak_count -= 1
        if self.weak_count <= 0 and self.ref_count <= 0:
            self._destroy_control_block()
        return self.weak_count
    
    def _destroy_object(self) -> None:
        """销毁对象"""
        if self.deleter:
            self.deleter(self.object)
        elif self.object and hasattr(self.object, '__del__'):
            # 尝试调用析构函数
            pass
        else:
            import gc
            if self.type_info == "数组":
                gc.free(self.object)  # 假设是 C 数组
            else:
                gc.free(self.object)
    
    def _release_weak(self) -> None:
        """释放弱引用"""
        if self.weak_count <= 0:
            self._destroy_control_block()
    
    def _destroy_control_block(self) -> None:
        """销毁控制块自身"""
        del self.object
        del self

@dataclass
class UniquePtr(Generic[T]):
    """独享指针"""
    _ptr: Optional[Any] = None
    _deleter: Optional[Callable] = None
    
    @classmethod
    def make(cls, value: T, deleter: Optional[Callable] = None) -> 'UniquePtr[T]':
        """创建独享指针"""
        ptr = cls()
        ptr._ptr = value
        ptr._deleter = deleter or (lambda x: None)
        return ptr
    
    def get(self) -> Optional[T]:
        """获取裸指针"""
        return self._ptr
    
    def release(self) -> Optional[T]:
        """释放所有权，返回裸指针"""
        ptr = self._ptr
        self._ptr = None
        return ptr
    
    def reset(self, new_ptr: Optional[T] = None) -> None:
        """重置指针"""
        if self._ptr is not None:
            self._deleter(self._ptr)
        self._ptr = new_ptr
    
    def swap(self, other: 'UniquePtr[T]') -> None:
        """交换所有权"""
        self._ptr, other._ptr = other._ptr, self._ptr
        self._deleter, other._deleter = other._deleter, self._deleter
    
    def __enter__(self) -> Optional[T]:
        return self._ptr
    
    def __exit__(self, *args) -> None:
        self.reset()
    
    def __bool__(self) -> bool:
        return self._ptr is not None
    
    def __del__(self) -> None:
        self.reset()

@dataclass
class SharedPtr(Generic[T]):
    """共享指针"""
    _ptr: Optional[Any] = None
    _control: Optional[ControlBlock] = None
    
    @classmethod
    def make(cls, value: T, deleter: Optional[Callable] = None) -> 'SharedPtr[T]':
        """创建共享指针"""
        ptr = cls()
        ptr._ptr = value
        ptr._control = ControlBlock(
            object=value,
            deleter=deleter,
            type_info=type(value).__name__,
        )
        return ptr
    
    @classmethod
    def from_existing(cls, ptr: Any, control: ControlBlock) -> 'SharedPtr[T]':
        """从已有指针创建（增加引用计数）"""
        shared = cls()
        shared._ptr = ptr
        shared._control = control
        control.add_ref()
        return shared
    
    def get(self) -> Optional[T]:
        """获取裸指针"""
        return self._ptr
    
    def ref_count(self) -> int:
        """获取引用计数"""
        return self._control.ref_count if self._control else 0
    
    def reset(self, new_ptr: Optional[T] = None) -> None:
        """重置指针"""
        if self._control:
            self._control.release()
        if new_ptr is not None:
            self._ptr = new_ptr
            self._control = ControlBlock(object=new_ptr)
        else:
            self._ptr = None
            self._control = None
    
    def to_weak(self) -> 'WeakPtr[T]':
        """转换为弱指针"""
        return WeakPtr.make(self._ptr, self._control)
    
    def __bool__(self) -> bool:
        return self._ptr is not None
    
    def __del__(self) -> None:
        if self._control:
            self._control.release()
    
    def __copy__(self) -> 'SharedPtr[T]':
        """浅拷贝 - 增加引用计数"""
        return SharedPtr.from_existing(self._ptr, self._control)

@dataclass
class WeakPtr(Generic[T]):
    """弱指针"""
    _ptr: Optional[Any] = None
    _control: Optional[ControlBlock] = None
    
    @classmethod
    def make(cls, ptr: Any, control: ControlBlock) -> 'WeakPtr[T]':
        """创建弱指针"""
        weak = cls()
        weak._ptr = ptr
        weak._control = control
        control.add_weak_ref()
        return weak
    
    def lock(self) -> Optional[SharedPtr[T]]:
        """升级为共享指针"""
        if self.expired():
            return None
        return SharedPtr.from_existing(self._ptr, self._control)
    
    def expired(self) -> bool:
        """检查对象是否已销毁"""
        return self._control is None or self._control.ref_count <= 0
    
    def ref_count(self) -> int:
        """获取引用计数"""
        return self._control.ref_count if self._control else 0
    
    def reset(self) -> None:
        """重置弱指针"""
        if self._control:
            self._control.release_weak()
        self._ptr = None
        self._control = None
```

### 3.3 循环引用检测

```python
# src/zhc/memory/cycle_detector.py

class CycleDetector:
    """循环引用检测器"""
    
    def __init__(self):
        self.visited: set = set()
        self.path: list = []
    
    def detect(self, shared_ptr: SharedPtr) -> bool:
        """检测是否存在循环引用"""
        self.visited.clear()
        self.path.clear()
        return self._dfs(shared_ptr._control, [])
    
    def _dfs(self, control: ControlBlock, path: list) -> bool:
        """深度优先搜索检测环"""
        if control is None:
            return False
        
        if id(control) in path:
            # 发现环
            return True
        
        if id(control) in self.visited:
            return False
        
        self.visited.add(id(control))
        path.append(id(control))
        
        # 检查控制块引用的对象中的 shared_ptr
        if hasattr(control.object, '__dict__'):
            for attr_value in control.object.__dict__.values():
                if isinstance(attr_value, SharedPtr):
                    if self._dfs(attr_value._control, path[:]):
                        return True
        
        return False
```

---

## 4. 实现计划

### 4.1 第一阶段：核心实现

**文件变更**:
- `src/zhc/memory/__init__.py` (新建)
- `src/zhc/memory/smart_ptr.py` (新建)

**任务**:
- [ ] 实现 `ControlBlock` 类
- [ ] 实现 `UniquePtr` 类
- [ ] 实现 `SharedPtr` 类
- [ ] 实现 `WeakPtr` 类

### 4.2 第二阶段：循环检测

**文件变更**:
- `src/zhc/memory/cycle_detector.py` (新建)

**任务**:
- [ ] 实现 `CycleDetector` 类
- [ ] 实现 `detect()` 方法

### 4.3 第三阶段：语法支持

**文件变更**:
- `src/zhc/parser/lexer.py` (更新)
- `src/zhc/parser/parser.py` (更新)

**任务**:
- [ ] 添加 `独享指针`、`共享指针`、`弱指针` 关键字
- [ ] 解析智能指针类型

### 4.4 第四阶段：语义分析

**文件变更**:
- `src/zhc/semantic/smart_ptr_checker.py` (新建)

**任务**:
- [ ] 智能指针类型检查
- [ ] 所有权转移检查
- [ ] 循环引用警告

### 4.5 第五阶段：代码生成

**文件变更**:
- `src/zhc/codegen/llvm_backend.py` (更新)

**任务**:
- [ ] 生成智能指针 IR
- [ ] 生成引用计数操作

### 4.6 第六阶段：C 运行时

**文件变更**:
- `src/zhc/lib/zhc_smart_ptr.h` (新建)
- `src/zhc/lib/zhc_smart_ptr.c` (新建)

**任务**:
- [ ] 实现 `zhc_unique_ptr_*`
- [ ] 实现 `zhc_shared_ptr_*`
- [ ] 实现 `zhc_weak_ptr_*`

---

## 5. 验收标准

- [ ] 支持 `独享指针` 独占所有权
- [ ] 支持 `共享指针` 共享所有权
- [ ] 支持 `弱指针` 不拥有所有权
- [ ] 引用计数正确
- [ ] 支持自定义删除器
- [ ] 支持循环引用检测
- [ ] 所有单元测试通过
