# P4-包管理-包仓库集成

## 功能概述

**优先级**: P4  
**功能模块**: 包管理  
**功能名称**: 包仓库集成  
**功能描述**: 实现包仓库接口和默认仓库集成  
**预估工时**: 4天  
**依赖项**: 包依赖管理、版本控制  
**状态**: 待开发

---

## 1. 需求分析

### 1.1 功能目标

实现包仓库系统，支持包的注册、查询、下载和管理：

1. **仓库接口**: 定义统一的仓库 API
2. **本地仓库**: 支持本地文件系统的包
3. **远程仓库**: 支持 HTTP/HTTPS 协议的远程仓库
4. **包索引**: 支持包的元数据查询
5. **包下载**: 支持包的下载和缓存

**示例使用**:
```bash
# 配置仓库
zhc repo add official https://packages.zhc.com
zhc repo add private https://private.zhc.com --auth token

# 搜索包
zhc search 网络库
# 输出:
# 网络库 v2.1.0 - 高性能网络库
# 网络库 v1.5.0 - 基础网络库

# 查看包信息
zhc info 网络库
# 输出:
# 名称: 网络库
# 版本: 2.1.0
# 作者: ZHC Team
# 描述: 高性能异步网络库
# 依赖: 日志工具 ^1.0.0
# 下载量: 10000+

# 下载包
zhc download 网络库 v2.1.0

# 发布包
zhc publish --repo official
```

### 1.2 仓库规范

#### 仓库结构

```
repository/
├── index.json           # 包索引
├── packages/
│   ├── pkg1/
│   │   ├── 1.0.0.zip    # 包文件
│   │   └── 1.0.0.json   # 包元数据
│   └── pkg2/
│       └── ...
└── auth/                # 认证信息（可选）
```

#### 包索引格式

```json
{
  "name": "package-name",
  "versions": [
    {
      "version": "2.1.0",
      "description": "包描述",
      "author": "作者",
      "license": "MIT",
      "homepage": "https://...",
      "repository": "https://...",
      "dependencies": {
        "日志工具": "^1.0.0"
      },
      "downloadUrl": "https://.../package-2.1.0.zip",
      "sha256": "abc123...",
      "publishedAt": "2024-01-01T00:00:00Z"
    }
  ]
}
```

### 1.3 边界情况

| 边界情况 | 处理方式 |
|---------|---------|
| 仓库连接失败 | 使用缓存，提示网络问题 |
| 包不存在 | 清晰的错误提示 |
| 版本不存在 | 列出可用版本 |
| 认证失败 | 提示重新认证 |
| 包签名验证失败 | 拒绝安装 |
| 下载中断 | 支持断点续传 |
| 仓库冲突 | 按优先级选择 |

---

## 2. 当前代码分析

### 2.1 相关文件

| 文件路径 | 作用 |
|---------|-----|
| `src/package/manager.py` | 包管理器 |
| `src/package/repository.py` | 仓库接口（待创建） |
| `src/package/cache.py` | 本地缓存 |

### 2.2 当前实现状态

**现状**: 包管理器已有基本框架，缺少仓库集成。

**缺失功能**:
- 仓库接口抽象
- 远程仓库支持
- 包索引查询
- 认证机制

---

## 3. 实现方案

### 3.1 设计决策

**问题**: 如何设计仓库系统？

**选择**: 分层架构 - Repository 接口 + 多实现

**架构设计**:
```
┌─────────────────────────────────────────────┐
│              PackageManager                  │
│  (包管理入口，协调各组件)                      │
└─────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────┐
│            RepositoryRegistry                │
│  (仓库注册表，管理多个仓库)                    │
└─────────────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        ▼                         ▼
┌───────────────┐        ┌───────────────┐
│ LocalRepository│        │ RemoteRepository│
│  (本地仓库)   │        │  (远程仓库)    │
└───────────────┘        └───────────────┘
        │                         │
        ▼                         ▼
   文件系统                   HTTP/HTTPS
```

**理由**:
1. 清晰的接口定义，便于扩展
2. 支持多个仓库并行查询
3. 解耦仓库实现细节
4. 便于测试和模拟

### 3.2 模块设计

#### 文件结构

```
src/package/
├── repository/
│   ├── __init__.py
│   ├── base.py          # 仓库基类
│   ├── registry.py      # 仓库注册表
│   ├── local.py         # 本地仓库实现
│   ├── remote.py        # 远程仓库实现
│   ├── index.py         # 包索引管理
│   └── auth.py          # 认证管理
└── manager.py           # 依赖项更新
```

### 3.3 实现步骤

#### 步骤1：定义仓库接口

```python
# src/package/repository/base.py

"""
包仓库基类

定义统一的仓库接口
"""

from typing import Dict, List, Optional, BinaryIO
from pathlib import Path
from abc import ABC, abstractmethod
from dataclasses import dataclass
from datetime import datetime

@dataclass
class PackageMetadata:
    """包元数据"""
    name: str
    version: str
    description: Optional[str] = None
    author: Optional[str] = None
    license: Optional[str] = None
    homepage: Optional[str] = None
    repository: Optional[str] = None
    dependencies: Dict[str, str] = None
    download_url: Optional[str] = None
    sha256: Optional[str] = None
    published_at: Optional[datetime] = None
    downloads: Optional[int] = None
    
    def __post_init__(self):
        if self.dependencies is None:
            self.dependencies = {}

@dataclass
class PackageSearchResult:
    """包搜索结果"""
    name: str
    description: Optional[str]
    version: str
    author: Optional[str]
    downloads: Optional[int]

class PackageRepository(ABC):
    """包仓库基类"""
    
    def __init__(self, url: str):
        self.url = url
        self.name = self._extract_name(url)
    
    @abstractmethod
    def get_versions(self, package_name: str) -> List['Version']:
        """获取包的所有可用版本"""
        pass
    
    @abstractmethod
    def get_metadata(self, package_name: str, version: 'Version') -> PackageMetadata:
        """获取包元数据"""
        pass
    
    @abstractmethod
    def get_dependencies(
        self,
        package_name: str,
        version: 'Version'
    ) -> Dict[str, str]:
        """获取包依赖"""
        pass
    
    @abstractmethod
    def download(
        self,
        package_name: str,
        version: 'Version',
        target_path: Path
    ) -> Path:
        """下载包到目标路径"""
        pass
    
    @abstractmethod
    def search(self, query: str) -> List[PackageSearchResult]:
        """搜索包"""
        pass
    
    @abstractmethod
    def exists(self, package_name: str, version: Optional['Version'] = None) -> bool:
        """检查包是否存在"""
        pass
    
    def get_source(self, package_name: str, version: 'Version') -> str:
        """获取包来源"""
        return self.url
    
    def _extract_name(self, url: str) -> str:
        """从 URL 提取仓库名称"""
        if '://' in url:
            return url.split('://')[1].split('/')[0]
        return url.split('/')[0]
    
    def __repr__(self) -> str:
        return f"{self.__class__.__name__}({self.name})"
```

#### 步骤2：实现本地仓库

```python
# src/package/repository/local.py

"""
本地仓库实现

从文件系统加载包
"""

from typing import Dict, List, Optional
from pathlib import Path
import json
import zipfile
import shutil
from datetime import datetime

from .base import PackageRepository, PackageMetadata, PackageSearchResult
from ..version import Version

class LocalRepository(PackageRepository):
    """本地仓库"""
    
    def __init__(self, path: Path):
        super().__init__(str(path))
        self.path = Path(path)
        self.index_path = self.path / 'index.json'
        self.packages_dir = self.path / 'packages'
        
        # 加载索引
        self._index = self._load_index()
    
    def _load_index(self) -> Dict:
        """加载包索引"""
        if self.index_path.exists():
            with open(self.index_path, 'r', encoding='utf-8') as f:
                return json.load(f)
        return {'packages': {}}
    
    def _save_index(self):
        """保存包索引"""
        self.index_path.parent.mkdir(parents=True, exist_ok=True)
        with open(self.index_path, 'w', encoding='utf-8') as f:
            json.dump(self._index, f, indent=2, ensure_ascii=False)
    
    def get_versions(self, package_name: str) -> List[Version]:
        """获取包的所有版本"""
        packages = self._index.get('packages', {})
        package_data = packages.get(package_name, {})
        versions = package_data.get('versions', {})
        
        return sorted([
            Version.parse(v) for v in versions.keys()
        ], reverse=True)
    
    def get_metadata(self, package_name: str, version: Version) -> PackageMetadata:
        """获取包元数据"""
        packages = self._index.get('packages', {})
        package_data = packages.get(package_name, {})
        version_data = package_data.get('versions', {}).get(str(version))
        
        if not version_data:
            raise PackageNotFoundError(
                f"包 {package_name} {version} 不存在"
            )
        
        return PackageMetadata(
            name=package_name,
            version=str(version),
            description=version_data.get('description'),
            author=version_data.get('author'),
            license=version_data.get('license'),
            homepage=version_data.get('homepage'),
            repository=version_data.get('repository'),
            dependencies=version_data.get('dependencies', {}),
            download_url=version_data.get('downloadUrl'),
            sha256=version_data.get('sha256'),
            published_at=version_data.get('publishedAt'),
            downloads=version_data.get('downloads')
        )
    
    def get_dependencies(
        self,
        package_name: str,
        version: Version
    ) -> Dict[str, str]:
        """获取包依赖"""
        metadata = self.get_metadata(package_name, version)
        return metadata.dependencies
    
    def download(
        self,
        package_name: str,
        version: Version,
        target_path: Path
    ) -> Path:
        """下载包到目标路径"""
        package_dir = self.packages_dir / package_name / str(version)
        archive_path = package_dir / f'{package_name}-{version}.zip'
        
        if not archive_path.exists():
            raise PackageNotFoundError(
                f"包文件 {package_name} {version} 不存在"
            )
        
        # 解压到目标路径
        target_path.mkdir(parents=True, exist_ok=True)
        
        with zipfile.ZipFile(archive_path, 'r') as zf:
            zf.extractall(target_path)
        
        return target_path
    
    def search(self, query: str) -> List[PackageSearchResult]:
        """搜索包"""
        results = []
        packages = self._index.get('packages', {})
        
        query_lower = query.lower()
        
        for name, package_data in packages.items():
            # 检查名称匹配
            if query_lower not in name.lower():
                continue
            
            # 获取最新版本信息
            versions = package_data.get('versions', {})
            if not versions:
                continue
            
            latest_version = max(versions.keys())
            version_data = versions[latest_version]
            
            results.append(PackageSearchResult(
                name=name,
                description=version_data.get('description'),
                version=latest_version,
                author=version_data.get('author'),
                downloads=version_data.get('downloads')
            ))
        
        return results
    
    def exists(self, package_name: str, version: Optional[Version] = None) -> bool:
        """检查包是否存在"""
        packages = self._index.get('packages', {})
        
        if package_name not in packages:
            return False
        
        if version is None:
            return True
        
        package_versions = packages[package_name].get('versions', {})
        return str(version) in package_versions
```

#### 步骤3：实现远程仓库（简化版）

```python
# src/package/repository/remote.py

"""
远程仓库实现

通过 HTTP/HTTPS 访问远程仓库
"""

from typing import Dict, List, Optional
from pathlib import Path
import requests
import hashlib
from urllib.parse import urljoin

from .base import PackageRepository, PackageMetadata, PackageSearchResult
from ..version import Version
from ..errors import NetworkError, PackageNotFoundError

class RemoteRepository(PackageRepository):
    """远程仓库"""
    
    def __init__(
        self,
        url: str,
        auth_token: Optional[str] = None,
        timeout: int = 30
    ):
        super().__init__(url)
        self.auth_token = auth_token
        self.timeout = timeout
        self.session = self._create_session()
    
    def _create_session(self) -> requests.Session:
        """创建 HTTP 会话"""
        session = requests.Session()
        session.timeout = self.timeout
        
        if self.auth_token:
            session.headers['Authorization'] = f'Bearer {self.auth_token}'
        
        session.headers['User-Agent'] = 'ZHC-Package-Manager/1.0'
        return session
    
    def get_versions(self, package_name: str) -> List[Version]:
        """获取包的所有可用版本"""
        try:
            response = self.session.get(f'{self.url}/packages/{package_name}')
            response.raise_for_status()
            data = response.json()
            
            versions = [Version.parse(v) for v in data.get('versions', [])]
            return sorted(versions, reverse=True)
            
        except requests.HTTPError as e:
            if e.response.status_code == 404:
                return []
            raise NetworkError(f"获取版本失败: {e}")
    
    def get_metadata(self, package_name: str, version: Version) -> PackageMetadata:
        """获取包元数据"""
        try:
            response = self.session.get(f'{self.url}/packages/{package_name}/{version}')
            response.raise_for_status()
            data = response.json()
            
            return PackageMetadata(
                name=data['name'],
                version=data['version'],
                description=data.get('description'),
                author=data.get('author'),
                license=data.get('license'),
                homepage=data.get('homepage'),
                repository=data.get('repository'),
                dependencies=data.get('dependencies', {}),
                download_url=data.get('downloadUrl'),
                sha256=data.get('sha256'),
                published_at=data.get('publishedAt'),
                downloads=data.get('downloads')
            )
            
        except requests.HTTPError as e:
            if e.response.status_code == 404:
                raise PackageNotFoundError(f"包 {package_name} {version} 不存在")
            raise NetworkError(f"获取元数据失败: {e}")
    
    def get_dependencies(self, package_name: str, version: Version) -> Dict[str, str]:
        """获取包依赖"""
        metadata = self.get_metadata(package_name, version)
        return metadata.dependencies
    
    def download(self, package_name: str, version: Version, target_path: Path) -> Path:
        """下载包到目标路径"""
        metadata = self.get_metadata(package_name, version)
        
        if not metadata.download_url:
            raise NetworkError(f"包 {package_name} {version} 没有下载链接")
        
        target_path.mkdir(parents=True, exist_ok=True)
        target_file = target_path / f'{package_name}-{version}.zip'
        
        response = self.session.get(metadata.download_url, stream=True)
        
        with open(target_file, 'wb') as f:
            for chunk in response.iter_content(chunk_size=8192):
                f.write(chunk)
        
        # 验证哈希
        if metadata.sha256:
            self._verify_hash(target_file, metadata.sha256)
        
        return target_file
    
    def _verify_hash(self, file_path: Path, expected_hash: str):
        """验证文件哈希"""
        sha256 = hashlib.sha256()
        
        with open(file_path, 'rb') as f:
            for chunk in iter(lambda: f.read(8192), b''):
                sha256.update(chunk)
        
        actual_hash = sha256.hexdigest()
        
        if actual_hash != expected_hash:
            raise NetworkError(f"文件哈希验证失败")
    
    def search(self, query: str) -> List[PackageSearchResult]:
        """搜索包"""
        try:
            response = self.session.get(f'{self.url}/search?q={query}')
            response.raise_for_status()
            data = response.json()
            
            results = []
            for item in data.get('results', []):
                results.append(PackageSearchResult(
                    name=item['name'],
                    description=item.get('description'),
                    version=item['version'],
                    author=item.get('author'),
                    downloads=item.get('downloads')
                ))
            
            return results
            
        except requests.HTTPError as e:
            raise NetworkError(f"搜索失败: {e}")
    
    def exists(self, package_name: str, version: Optional[Version] = None) -> bool:
        """检查包是否存在"""
        try:
            if version is None:
                response = self.session.get(f'{self.url}/packages/{package_name}')
                return response.status_code == 200
            else:
                self.get_metadata(package_name, version)
                return True
        except PackageNotFoundError:
            return False
        except NetworkError:
            return False
```

### 3.4 完整修改清单

| 序号 | 文件 | 修改内容 |
|-----|------|---------|
| 1 | `src/package/repository/__init__.py` | 创建模块 |
| 2 | `src/package/repository/base.py` | 仓库基类 |
| 3 | `src/package/repository/local.py` | 本地仓库实现 |
| 4 | `src/package/repository/remote.py` | 远程仓库实现 |
| 5 | `src/package/repository/registry.py` | 仓库注册表 |
| 6 | `src/package/repository/auth.py` | 认证管理 |
| 7 | `src/package/repository/errors.py` | 错误定义 |
| 8 | `src/package/errors.py` | 更新错误定义 |
| 9 | `src/cli/repo_cli.py` | CLI 命令 |

---

## 4. 测试用例

### 4.1 本地仓库测试

```python
# tests/package/test_local_repository.py

def test_local_repository_register(tmp_path):
    """测试包注册"""
    repo = LocalRepository(tmp_path)
    
    metadata = {
        'description': '测试包',
        'author': 'Test',
        'license': 'MIT',
        'dependencies': {}
    }
    
    archive_path = tmp_path / 'test-1.0.0.zip'
    archive_path.touch()
    
    repo.register_package('测试包', Version.parse('1.0.0'), metadata, archive_path)
    
    assert repo.exists('测试包')
    versions = repo.get_versions('测试包')
    assert len(versions) == 1
    assert versions[0] == Version.parse('1.0.0')

def test_local_repository_search(tmp_path):
    """测试包搜索"""
    # 创建一些包...
    
    repo = LocalRepository(tmp_path)
    results = repo.search('网络')
    
    assert len(results) > 0
    assert any(r.name == '网络库' for r in results)
```

### 4.2 远程仓库测试

```python
# tests/package/test_remote_repository.py

def test_remote_repository_get_versions(requests_mock):
    """测试获取版本列表"""
    requests_mock.get(
        'https://example.com/packages/网络库',
        json={'versions': ['2.0.0', '1.5.0', '1.0.0']}
    )
    
    repo = RemoteRepository('https://example.com')
    versions = repo.get_versions('网络库')
    
    assert len(versions) == 3
    assert versions[0] == Version.parse('2.0.0')
```

---

## 5. 实现优先级

### 5.1 开发顺序

1. **第一步**: 定义仓库接口（0.5天）
2. **第二步**: 实现本地仓库（1天）
3. **第三步**: 实现远程仓库（1.5天）
4. **第四步**: 实现仓库注册表（1天）
5. **第五步**: CLI 集成（0.5天）

### 5.2 预估时间

| 步骤 | 预估时间 |
|-----|---------|
| 仓库接口定义 | 0.5天 |
| 本地仓库实现 | 1天 |
| 远程仓库实现 | 1.5天 |
| 仓库注册表 | 1天 |
| CLI 集成 | 0.5天 |
| **总计** | **4.5天** |

---

## 6. 验收标准

- [ ] Repository 接口定义完整
- [ ] LocalRepository 实现正确
- [ ] RemoteRepository 实现正确
- [ ] RepositoryRegistry 管理多个仓库
- [ ] 支持仓库优先级
- [ ] 支持仓库搜索
- [ ] 支持认证机制
- [ ] 所有测试用例通过

---

## 7. 后续扩展

### 7.1 高级特性

- 镜像仓库支持
- CDN 加速
- 包签名验证
- 增量更新

### 7.2 企业功能

- LDAP 集成
- 审计日志
- 访问控制

---

## 8. 相关文档

- npm Registry API: https://github.com/npm/registry
- Verdaccio: https://verdaccio.org/
- 语义化版本: https://semver.org/

---

**创建日期**: 2026-04-09  
**最后更新**: 2026-04-09
