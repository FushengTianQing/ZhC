# 技术债务重构计划

**创建日期**: 2026-04-03
**目标**: 改进代码结构，提高可维护性

---

## 重构任务清单

### 1. parser/parser.py 重构 (1047行)

**问题**: 单文件1000+行，职责过多

**当前结构**:
- ParseError (异常类)
- ErrorRecovery (错误恢复)
- Parser (主解析器，58个方法)

**目标结构**:
```
parser/
├── __init__.py
├── parser.py          # Parser主类（精简）
├── declaration_parser.py  # 声明解析器
├── statement_parser.py    # 语句解析器
├── expression_parser.py   # 表达式解析器
├── error_recovery.py     # 错误恢复
└── exceptions.py        # 异常类
```

**拆分方法**:
1. `declaration_parser.py`: parse_module_decl, parse_import_decl, parse_function_decl, parse_struct_decl, parse_variable_decl, parse_const_decl, parse_param_decl
2. `statement_parser.py`: parse_statement, parse_block, parse_if_stmt, parse_while_stmt, parse_for_stmt, parse_break_stmt, parse_continue_stmt, parse_return_stmt, parse_expr_stmt
3. `expression_parser.py`: parse_type, parse_expression, parse_assignment, parse_or, parse_and, parse_equality, parse_comparison, parse_addition, parse_multiplication, parse_unary, parse_postfix, parse_primary

---

### 2. compiler/pipeline.py 重构 (680行)

**问题**: 导入路径复杂，相对导入不一致

**当前问题**:
```python
try:
    from ..day2.module_parser import ModuleParser
    from ..day3.code_converter import CodeConverter
    from ..day3.error_handler import ErrorHandler
    from ..day4.dependency_resolver import DependencyResolver, MultiFileIntegrator
except ImportError as e:
    print(f"⚠️ 导入模块时出错: {e}")
    print("正在使用简化版本...")
```

**目标**:
1. 统一使用绝对导入
2. 添加导入错误处理
3. 简化Fallback机制

---

### 3. converter/code.py 重构 (603行)

**问题**: 转换逻辑与错误处理耦合

**当前结构**:
```python
class CodeConverter:
    def __init__(self, scope_manager=None):
        self.errors = []
        self.warnings = []

    def add_error(self, error_type: str, message: str, line_no: int = -1):
        self.errors.append(...)

    def add_warning(self, ...):
        self.warnings.append(...)

    # 转换方法...
```

**目标结构**:
```
converter/
├── __init__.py
├── code_converter.py   # CodeConverter（精简，只含转换逻辑）
├── error_handler.py    # 错误处理分离
└── ...
```

**拆分**:
1. 创建独立的ErrorHandler类处理错误收集
2. CodeConverter注入ErrorHandler依赖
3. 保持转换逻辑不变

---

## 实施顺序

1. **converter/code.py** - 最简单，先处理
2. **compiler/pipeline.py** - 导入路径修复
3. **parser/parser.py** - 最复杂，最后处理

---

## 验收标准

- [ ] parser/parser.py 拆分为4个文件
- [ ] compiler/pipeline.py 导入路径统一
- [ ] converter/code.py 错误处理分离
- [ ] 所有测试仍然通过
- [ ] 无功能损失