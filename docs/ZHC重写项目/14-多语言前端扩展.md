# 第十四章：新增功能——多语言前端扩展（C + Python）

**版本**: v2.0 | **日期**: 2026-04-12

> **本章节基于重写v3.md 补全，整合了多语言前端扩展的技术实现方案。**

---

## 14.1 功能定位与战略价值

### 14.1.1 为什么做多语言前端

**C 前端**的战略价值：
- 复用 LLVM 优化流水线（200+ Pass），C 代码也能享受极致优化
- 统一多语言工具链（`.zhc` + `.c` + `.py` → 同一后端）
- 一次开发，ZHC/C/Python 三种语言共享语义分析 + IR 优化 + LLVM 后端
- 技术验证：为 Python 前端铺路（低风险练手）

**Python 前端**的战略价值：
- 编译 Python 为机器码，性能提升 10~100x
- Python 脚本编译为独立二进制，无需 Python 运行时（零依赖部署）
- 机器码反编译难度远高于 .pyc（代码保护）
- 场景覆盖：性能关键代码加速、嵌入式部署、商业代码分发

### 14.1.2 多语言架构概览

```
┌──────────────────────────────────────────────────────────────────────┐
│                          ZhC 多语言编译器架构                           │
│                                                                      │
│  ┌──────────────────────┬──────────────────────┬────────────────────┐  │
│  │      ZHC 前端        │       C 前端        │    Python 前端   │  │
│  │      (已有)          │      (新增 C)        │    (新增 Python)  │  │
│  ├──────────────────────┼──────────────────────┼────────────────────┤  │
│  │  keywords.py         │  zhc_c/keywords.py   │ zhc_python/       │  │
│  │  lexer.py            │  zhc_c/lexer.py      │   type_mapping.py │  │
│  │  parser.py           │  zhc_c/parser.py     │   builtin_map.py  │  │
│  │                      │  zhc_c/ast_bridge.py │   parser.py       │  │
│  └──────────┬───────────┴──────────┬───────────┴─────────┬──────────┘  │
│             │                      │                      │              │
│             └──────────────────────┼──────────────────────┘              │
│                                    ▼                                     │
│                      ┌──────────────────────────────┐                   │
│                      │      统一编译流水线            │                   │
│                      │                              │                   │
│                      │  SemanticAnalyzer (共用)      │                   │
│                      │  IRGenerator (共用)           │                   │
│                      │  IROptimizer (共用)           │                   │
│                      │  LLVM Backend (共用)          │                   │
│                      └──────────────────────────────┘                   │
└──────────────────────────────────────────────────────────────────────┘

输入文件格式：
  .zhc  →  中文C语法   → 机器码  （已有）
  .c    →  标准C语法   → 机器码  （新增 C 前端）
  .py   →  Python语法  → 机器码  （新增 Python 前端）
```

---

## 14.2 C 前端：技术实现

### 14.2.1 目录结构

```
src/zhc_c/                          # C 前端包（新增）
├── __init__.py
├── tokens.py                       # C Token 类型定义
├── lexer.py                        # C 词法分析器
├── parser.py                       # C 语法分析器
├── ast_bridge.py                   # C AST → ZHC 通用 AST 桥接器
└── keywords.py                     # C 关键词表
```

### 14.2.2 C 关键词表

```python
# src/zhc_c/keywords.py

C_KEYWORDS = {
    # C11/C17 标准关键词
    "auto": "auto", "break": "break", "case": "case",
    "char": "char", "const": "const", "continue": "continue",
    "default": "default", "do": "do", "double": "double",
    "else": "else", "enum": "enum", "extern": "extern",
    "float": "float", "for": "for", "goto": "goto",
    "if": "if", "inline": "inline", "int": "int",
    "long": "long", "register": "register", "restrict": "restrict",
    "return": "return", "short": "short", "signed": "signed",
    "sizeof": "sizeof", "static": "static", "struct": "struct",
    "switch": "switch", "typedef": "typedef", "union": "union",
    "unsigned": "unsigned", "void": "void", "volatile": "volatile",
    "while": "while",

    # C99 布尔和复数类型
    "_Bool": "_Bool", "_Complex": "_Complex", "_Imaginary": "_Imaginary",

    # C11 线程和对齐
    "_Thread_local": "_Thread_local", "_Alignas": "_Alignas",
    "_Alignof": "_Alignof", "_Atomic": "_Atomic",
    "_Static_assert": "_Static_assert", "_Noreturn": "_Noreturn",

    # 扩展关键词
    "__builtin_va_list": "__builtin_va_list",
    "__attribute__": "__attribute__",
}
```

### 14.2.3 C AST → ZHC AST 桥接器

```python
# src/zhc_c/ast_bridge.py
# C AST → ZHC 通用 AST 桥接器

# C 类型名 → ZHC 类型名的映射
C_TYPE_TO_ZHC = {
    "int": "整数型", "char": "字符型",
    "float": "浮点型", "double": "双精度浮点型",
    "void": "空型", "long": "长整数型",
    "short": "短整数型", "signed": "有符号整数型",
    "unsigned": "无符号整数型",
    "_Bool": "逻辑型",
    "FILE": "文件型", "FILE*": "文件型*",
    # stdint.h 类型映射
    "int8_t": "整数型", "int16_t": "整数型",
    "int32_t": "长整数型", "int64_t": "长长整数型",
    "uint8_t": "无符号整数型", "size_t": "无符号长整数型",
}

class CAstBridge:
    """C AST → ZHC 通用 AST 桥接器
    
    设计原则：
    - 输入：C Parser 生成的中间 AST
    - 输出：与 ZHC Parser 输出一致的 AST 节点
    - 后续流水线（语义分析、IR生成、后端）完全无感知
    """

    def transform(self, c_ast) -> ProgramNode:
        """执行 C AST → ZHC AST 转换"""
        declarations = []
        
        for decl in c_ast.declarations:
            if isinstance(decl, FunctionDecl):
                declarations.append(self._transform_function(decl))
            elif isinstance(decl, GlobalVar):
                declarations.append(self._transform_global_variable(decl))
            elif isinstance(decl, (StructDecl, UnionDecl, EnumDecl)):
                declarations.append(self._transform_type_decl(decl))
        
        return ProgramNode(declarations=declarations)

    def _transform_function(self, c_func) -> FunctionDeclNode:
        """函数声明转换

        C:  int add(int a, int b) { return a + b; }
        ZHC: 整数型 add(整数型 a, 整数型 b) {
                  返回 a + b;
              }
        """
        return_type = self._transform_type(c_func.return_type)
        params = [self._transform_param(p) for p in c_func.params]
        body = self._transform_statement(c_func.body)

        return FunctionDeclNode(
            name=c_func.name,
            return_type=return_type,
            params=params,
            body=body,
        )

    def _transform_type(self, c_type) -> ASTNode:
        """C 类型 → ZHC 类型"""
        if isinstance(c_type, PointerType):
            base = self._transform_type(c_type.base_type)
            return PointerTypeNode(base)
        elif isinstance(c_type, ArrayType):
            base = self._transform_type(c_type.base_type)
            return ArrayTypeNode(base, c_type.size)
        elif isinstance(c_type, StructType):
            return StructTypeNode(c_type.name)
        else:
            zhc_name = C_TYPE_TO_ZHC.get(c_type.name, c_type.name)
            return PrimitiveTypeNode(zhc_name)
```

---

## 14.3 Python 前端：技术实现

### 14.3.1 目录结构

```
src/zhc_python/                          # Python 前端包（新增）
├── __init__.py
├── tokens.py                           # Python Token 定义
├── lexer.py                            # Python 词法分析器
├── parser.py                           # Python → ZHC AST 转换器
├── type_mapping.py                     # Python 类型 → ZHC 类型映射
├── type_inference.py                    # Python 动态类型静态推导
├── builtin_mappings.py                  # Python 内置函数映射表
└── transformers.py                      # 高级语法转换器
    #   · 列表推导 → 循环
    #   · 生成器 → 协程
    #   · with 语句 → 初始化/清理
```

### 14.3.2 Python 类型系统 → ZHC 类型系统

```python
# src/zhc_python/type_mapping.py

PYTHON_TYPE_TO_ZHC = {
    # 基础类型（可静态映射）
    "int": "长整数型",           # Python int → C long long（64位）
    "float": "双精度浮点型",    # Python float → C double
    "bool": "逻辑型",           # Python bool → C _Bool
    "None": "空型",             # Python None → C void*

    # 字符串（UTF-8 指针）
    "str": "结构体 PyString*",  # Python str → 自定义 PyString 结构体

    # 复合类型（需要运行时实现）
    "list": "结构体 PyList*",   # Python list → 自定义结构体
    "tuple": "结构体 PyTuple*", # Python tuple → 自定义结构体
    "dict": "结构体 PyDict*",   # Python dict → 自定义结构体
    "set": "结构体 PySet*",     # Python set → 自定义结构体

    # 特殊类型
    "object": "结构体 PyObject*",  # 一切皆对象
    "bytes": "字符型*",            # Python bytes → C char*
    "type": "结构体 PyType*",      # 类型对象
}

# Python 动态特性处理策略
DYNAMIC_FEATURE_STRATEGIES = {
    "变量类型动态": "尽力静态推导 + 运行时类型检查兜底",
    "多态/鸭子类型": "编译时检测调用链 + 函数指针表（vtable）模拟",
    "属性动态添加": "__dict__ 哈希表模拟（性能损耗 3~5x）",
    "eval/exec": "受限支持（静态可分析的表达式/语句）",
    "GIL": "无 GIL，直接编译为多线程（用户需手动处理同步）",
    "垃圾回收": "引用计数（retain/release）替代 GC",
}
```

### 14.3.3 Python 内置函数映射

```python
# src/zhc_python/builtin_mappings.py

# Python 内置函数 → ZHC/C 函数映射
BUILTIN_FUNCTION_MAP = {
    # 直接映射（ZHC/C 有等价实现）
    "print": "打印",
    "abs": "绝对值",
    "min": "最小值",
    "max": "最大值",
    "len": "长度",
    "chr": "字符码转字符",
    "ord": "字符转码点",
    "round": "四舍五入",
    "pow": "幂函数",
    "hex": "转十六进制",
    "oct": "转八进制",
    "bin": "转二进制",

    # 需要 C 库实现
    "input": "zhc_input",          # → 标准输入读取
    "open": "zhc_fopen",           # → stdio fopen
    "range": "zhc_range",          # → 生成数组/迭代器
    "enumerate": "zhc_enumerate",  # → 索引+值对
    "zip": "zhc_zip",              # → 合并迭代器
    "map": "zhc_map",               # → 函数映射
    "filter": "zhc_filter",        # → 函数过滤

    # Python 语义重新实现
    "int": "zhc_py_int",            # 宽松类型转换
    "str": "zhc_py_str",            # 统一字符串
    "list": "zhc_py_list",
    "dict": "zhc_py_dict",
    "bool": "zhc_py_bool",
    "isinstance": "zhc_is_instance",
    "hasattr": "zhc_has_attr",
    "getattr": "zhc_get_attr",
    "setattr": "zhc_set_attr",
}
```

---

## 14.4 Python C 运行时库

```c
/* src/zhc/lib/zhc_python_stdlib.c
 * Python 内置类型的 C 实现
 * 策略：一切皆对象 + 引用计数
 */

#include "zhc_python_stdlib.h"
#include <stdlib.h>
#include <string.h>
#include <wchar.h>

/* PyObject 基类 */
typedef struct PyObject {
    PyTypeObject* ob_type;
    int ob_refcnt;
} PyObject;

#define Py_INCREF(op)   ((op)->ob_refcnt++)
#define Py_DECREF(op)  do { if (--((op)->ob_refcnt) <= 0) PyObject_Free(op); } while(0)

/* PyList 实现 */
typedef struct {
    PyObject ob_base;
    PyObject** items;
    Py_ssize_t allocated;
    Py_ssize_t size;
} PyList;

PyObject* zhc_list_create(Py_ssize_t initial_size);
Py_ssize_t zhc_len(PyObject* obj);
int zhc_list_append(PyObject* obj, PyObject* item);

/* PyDict 实现 */
typedef struct {
    PyObject ob_base;
    PyDictEntry* entries;
    Py_ssize_t size;
    Py_ssize_t allocated;
} PyDict;

PyObject* zhc_dict_create(void);
PyObject* zhc_dict_get(PyObject* dict, PyObject* key);
int zhc_dict_set(PyObject* dict, PyObject* key, PyObject* value);

/* PyString 实现 */
typedef struct {
    PyObject ob_base;
    char* data;              // UTF-8 编码
    Py_ssize_t length;       // 字符数
    Py_ssize_t hash;
} PyString;

/* Python 内置函数 */
PyObject* zhc_input(PyObject* prompt);
PyObject* zhc_range(Py_ssize_t start, Py_ssize_t stop, Py_ssize_t step);
int zhc_is_instance(PyObject* obj, const char* type_name);

/* 类型对象 */
typedef struct PyTypeObject {
    const char* tp_name;
    Py_ssize_t tp_basicsize;
} PyTypeObject;

PyTypeObject PyList_Type = { .tp_name = "list", .tp_basicsize = sizeof(PyList) };
PyTypeObject PyDict_Type = { .tp_name = "dict", .tp_basicsize = sizeof(PyDict) };
PyTypeObject PyStr_Type = { .tp_name = "str", .tp_basicsize = sizeof(PyString) };
```

---

## 14.5 多语言前端路由

```python
# src/zhc/compiler/pipeline.py

class CompilationPipeline:
    """多语言编译流水线"""

    FRONTENDS = {
        "zhc": "zhc_frontend.ZhcFrontend",
        "c": "zhc_c_frontend.CFrontend",
        "python": "zhc_python_frontend.PythonFrontend",
    }

    def __init__(self, backend="llvm"):
        self.semantic_analyzer = SemanticAnalyzer()
        self.ir_generator = IRGenerator()
        self.ir_optimizer = IROptimizer()
        self.backend = self._create_backend(backend)

    def run(self, source: str, source_path: str, lang: str = "auto") -> CompileResult:
        # 语言检测
        if lang == "auto":
            lang = LanguageDetector.detect(source_path, source)

        # 前端路由
        frontend = self._get_frontend(lang)
        ast = frontend.parse(source)

        # 后续流水线复用
        typed_ast = self.semantic_analyzer.analyze(ast)
        ir = self.ir_generator.generate(typed_ast)
        optimized_ir = self.ir_optimizer.optimize(ir)

        return self.backend.compile(optimized_ir)

    def _get_frontend(self, lang: str):
        if lang == "zhc":
            return ZhcFrontend()
        elif lang == "c":
            return CFrontend()
        elif lang == "python":
            return PythonFrontend()
        else:
            raise BackendError(f"不支持的语言: {lang}")
```

---

## 14.6 工时估算

| 阶段 | 任务 | 预估工时 |
|:---|:---|:---:|
| **C 前端** | C Lexer + Keywords | 4h |
| **C 前端** | C Parser + AST 桥接 | 12h |
| **C 前端** | C 前端测试 | 4h |
| **Python 前端** | Python 类型映射 + 内置函数映射 | 8h |
| **Python 前端** | Python 类型推导引擎 | 6h |
| **Python 前端** | Python Parser（核心） | 20h |
| **Python 前端** | Python stdlib C 实现 | 16h |
| **Python 前端** | IR 扩展（Python 特有指令） | 6h |
| **Python 前端** | Python 前端测试 | 8h |
| | **合计** | **~84h（约 2~3 周）** |

---

## 14.7 Python 支持范围

**✅ 完全支持（v1.0）**
```
变量声明与基本类型（int, float, str, bool, list, dict）
函数定义与调用
if/elif/else 条件判断
for/while 循环（支持 range()）
return 语句
print() / input()
基本运算符（+ - * / % == != < > and or not）
字符串操作（+ 拼接，[] 索引，in 判断）
列表操作（append, len, 切片）
注释（# 单行，""" 多行）
```

**⚠️ 需要运行时检查（v1.0）**
```
动态类型变量（尽力推导，运行时类型检查）
动态属性赋值（使用 __dict__ 模拟）
```

**❌ 暂不支持（v1.0，后续评估）**
```
import / from（需要完整的模块系统）
class 继承（多态需要更复杂的 vtable 实现）
生成器 / yield（协程已有基础设施，可扩展）
装饰器
with 语句（需上下文管理器协议）
正则表达式 re 模块
网络请求 http 模块
NumPy / Pandas / TensorFlow 等 C 扩展依赖
```
